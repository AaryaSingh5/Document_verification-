# Required backend dependencies for Suraksha Setu Document Verification Module
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
pydantic>=2.6.0
python-multipart>=0.0.9
httpx>=0.27.0

# Testing dependencies
pytest>=8.0.0
pytest-asyncio>=0.23.0

# Optional: Required only when running OCR_MODE=cloud_vision with Google Cloud Vision API
# google-cloud-vision>=3.7.0

# OpenCV and Mediapipe for image preprocessing, deskewing, and face match / liveness
opencv-contrib-python>=4.8.0
mediapipe>=0.10.0
Pillow>=10.0.0
